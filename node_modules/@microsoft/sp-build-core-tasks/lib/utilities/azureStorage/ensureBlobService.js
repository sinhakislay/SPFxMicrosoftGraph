"use strict";
/**
 * @file ensureBlobService.d.ts
 * @Copyright (c) Microsoft Corporation.  All rights reserved.
 *
 * Connects to the Azure Blob Storage service and returns a connection object
 */
Object.defineProperty(exports, "__esModule", { value: true });
const azure = require("azure-storage");
function ensureBlobService(account, accountKey) {
    return new Promise((resolve, reject) => {
        try {
            const blobService = azure.createBlobService(account, accountKey).withFilter(new azure.ExponentialRetryPolicyFilter());
            resolve(blobService);
        }
        catch (error) {
            reject(new Error(error));
        }
    });
}
exports.default = ensureBlobService;
//# sourceMappingURL=ensureBlobService.js.map