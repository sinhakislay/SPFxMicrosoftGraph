/**
 * @copyright Microsoft Corporation. All rights reserved.
 */
import { IQos } from './IQos';
import { IQosStartEvent } from './IQosStartEvent';
import { IQosEndEvent } from './IQosEndEvent';
/**
 * QosLogger allows multiple QosMonitors to be deferred processed.
 * @internal
 */
export default class QosLogger {
    private static _instance;
    private _qosEvents;
    private _deferExecutor;
    private _shouldProcessInRealTime;
    private _qosLogger;
    static readonly instance: QosLogger;
    constructor();
    shouldProcessInRealTime: boolean;
    setLogger(qosLogger: IQos): void;
    getQosEvent(key: string): IQos;
    startQosMonitor(key: string, startData: IQosStartEvent): void;
    writeQosEndResult(key: string, endData: IQosEndEvent): void;
    private _startMonitor;
    private _endMonitor;
    private initializeLogger;
}
//# sourceMappingURL=QosLogger.d.ts.map