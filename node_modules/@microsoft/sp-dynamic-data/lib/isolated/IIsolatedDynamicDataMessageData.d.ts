import { IDynamicDataPropertyDefinition } from '../interfaces/IDynamicDataPropertyDefinition';
import { IDynamicDataSource } from '../interfaces/IDynamicDataSource';
import { IDynamicDataEventDefinition } from '../interfaces/IDynamicDataEventDefinition';
/**
 * @internal
 */
export declare type IsolatedDynamicDataMessageData = IIsolatedDynamicDataAllSources | IIsolatedDynamicDataPropertyDefinitions | IIsolatedDynamicDataPropertyValue | IIsolatedDynamicDataMessageSource;
/**
 * DynamicData message contract to handle the api call to get all the available sources
 * from the parent window using PostMessage interaction. *
 * @internal
 */
export interface IIsolatedDynamicDataAllSources {
    /**
     * List of readonly sources.
     */
    sources: ReadonlyArray<IDynamicDataSource>;
}
/**
 * The base interface used to handle the events related to dynamic data source.
 * @internal
 */
export interface IIsolatedDynamicDataMessageSource {
    /**
     * Source Id.
     */
    sourceId: string;
    /**
     * Error message from the error thrown on an operation in the parent window.
     */
    errorMessage?: string;
}
/**
 * Interface used to handle the events related to property changes on the dynamic data source.
 * @internal
 */
export interface IIsolatedDynamicDataMessageProperty extends IIsolatedDynamicDataMessageSource {
    /**
     * Property Id.
     */
    propertyId: string;
}
/**
 * DynamicData message contract to handle the api call to get all the property definitions
 * of a specific source, from the parent window using PostMessage interaction.
 * @internal
 */
export interface IIsolatedDynamicDataPropertyDefinitions extends IIsolatedDynamicDataMessageSource {
    /**
     * List of readonly property definitions.
     */
    propertyDefintions: ReadonlyArray<IDynamicDataPropertyDefinition>;
}
/**
 * DynamicData message contract to handle the api call to get the associated property value
 * of a specific property on a source, from the parent window using PostMessage interaction.
 * @internal
 */
export interface IIsolatedDynamicDataPropertyValue extends IIsolatedDynamicDataMessageSource {
    /**
     * Property Id.
     */
    propertyId: string;
    /**
     * Property value.
     */
    propertyValue: any;
}
/**
 * DynamicData message contract to handle the api call to get the allowed events on the
 * specified source from the parent window using PostMessage interaction.
 * @internal
 */
export interface IIsolatedDynamicDataAllowedEvents extends IIsolatedDynamicDataMessageSource {
    /**
     * Returns list of allowed events on the dynamic data source.
     */
    allowedEvents?: ReadonlyArray<IDynamicDataEventDefinition>;
}
//# sourceMappingURL=IIsolatedDynamicDataMessageData.d.ts.map