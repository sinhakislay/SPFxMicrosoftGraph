import { ServiceScope } from '@microsoft/sp-core-library';
import { IFetchProvider } from './FetchProvider';
import { IHttpClientOptions } from './HttpClient';
import HttpClientConfiguration from './HttpClientConfiguration';
/**
 * Private - this class contains some internal code shared between HttpClient
 * and SPHttpClient.
 */
export default class HttpClientHelper {
    private static readonly HTTP_CLIENT_CLASS_NAME_FETCH;
    /**
     * Clones the original options and sets the method to the passed in method name.
     */
    static overrideHttpMethod(options: IHttpClientOptions | undefined, httpRequestMethod: string): IHttpClientOptions;
    /**
     * All network requests are routed through this method, which calls the
     * underlying IFetchProvider.fetch().
     */
    static fetchCore(configuration: HttpClientConfiguration, request: Request, serviceScope: ServiceScope, fetchProvider: IFetchProvider, logSource: string): Promise<Response>;
}
//# sourceMappingURL=HttpClientHelper.d.ts.map