/**
 * Object that represents the data necessary to build an `SPFeatureInfo` object.
 * @internal
 */
export interface ISPFeatureInfoData {
    [key: string]: {
        Version: number;
        Enabled: boolean;
    };
}
/**
 * JSON serializable object.
 * @internal
 */
export interface ISPFeatureData {
    readonly version: number;
    readonly enabled: boolean;
}
/**
  * This class is primarily used with the `PageContext` class. It provides contextual
  * information for the features enabled on the SharePoint site that hosts the page.
  *
  * @internal
  */
export default class SPFeatureInfo {
    private _features;
    /**
     * @param data - Raw JSON data from the html file
     */
    constructor(data: ISPFeatureInfoData);
    /**
     * Returns a JSON serializable object.
     */
    _serialize(): ISPFeatureInfoData;
    /**
     * Returns feature information for a given feature.
     * @param featureName - Name of a feature to lookup
     */
    get(featureName: string): ISPFeatureData | undefined;
}
//# sourceMappingURL=SPFeatureInfo.d.ts.map