import { ServiceScope } from '@microsoft/sp-core-library';
import { IPartialTheme } from '@microsoft/office-ui-fabric-react-bundle';
import { IThemeProvider } from '@ms/odsp-datasources/lib/providers/theming/IThemeProvider';
import { ISpPageContext } from '@ms/odsp-datasources/lib/interfaces/ISpPageContext';
import { Promise as ODSPPromise } from '@ms/odsp-utilities-bundle';
/**
 * @internal
 */
export declare type _IODSPThemeDataSource = import('@ms/odsp-datasources/lib/dataSources/theming/IThemeDataSource').default;
/**
 * The class controls applying a theme to the current Page.
 * The theme is a web level setting, if a theme is applied this class with call loadThemedStyles
 * which will update the styles that are registered with load-themed-styles.
 *
 * @internal
 */
export default class SPThemeProvider {
    private _pageContext;
    private _themeFactoryProvider;
    constructor(serviceScope: ServiceScope);
    loadThemedStyles(forceUpdate?: boolean): Promise<IPartialTheme>;
    protected _getThemeProvider(pageContext: ISpPageContext | undefined): IThemeProvider;
    protected _getThemeDataSource(): ODSPPromise<_IODSPThemeDataSource>;
}
//# sourceMappingURL=SPThemeProvider.d.ts.map